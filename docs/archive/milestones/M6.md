# M6 · Premium coherence pass (Phase 2)

**Status:** ✅ Complete · **Branch:** `feat/roleproof-milestones` · **Date:** 2026-07-01

> One accent system, no rainbow chips, no repeated sermons — journey stage read from
> position and the rail, not from hue.

## What shipped

| Area | Change | File(s) |
|---|---|---|
| Palette collapse | `Tone` narrowed from **9 hues → 4**: `neutral · green · amber · rose`. `green` is the proof accent (`bg-proof`/`proof-soft`/`proof-deep`). The sky/violet/purple/teal/brand rainbow is retired | `lib/ui.ts` |
| Status by position | `STATUS_META` remapped: in-progress states are neutral, `hold` is amber, `ready`/`applied` are proof-green. Journey stage is conveyed by the rail + order, not colour | `lib/ui.ts` |
| CTA accent | Journey next-action tones (teal/purple/brand/violet) collapsed to the single proof accent | `lib/journey.ts` |
| Badge | Evidence-source badge's violet/brand → neutral/green | `components/evidence-editor.tsx` |
| Trust copy | Removed the redundant "enters your graph only if you approve" caption sitting next to the "AI draft → your approval" pill — one statement per surface | `components/coach-queue.tsx` |

## Type roles (audited — already in place)

- **Instrument Serif** = editorial display (serif) · **Hanken Grotesk** = UI (sans) · **JetBrains Mono** =
  machinery/ref-codes/scores (mono). Confirmed in `tailwind.config.ts` + `app/layout.tsx`; no rogue font
  usage introduced. Radius/shadow are already centralised on `rounded-card` / `shadow-card`.

## Verification

- `npm run typecheck` → pass (no dangling references to the removed tones). `npm test` → 44 pass. `npm run build` → pass (proof classes resolve).

## Code review (agent, simplify pass) — findings & resolutions

Reviewer verdict: clean, no blocking issues — confirmed no dangling retired-tone references, the proof
Tailwind classes resolve, the `brand-*` ramp *is* the proof-green ramp (not rainbow leftover), and the
status labels disambiguate the now-shared tones. Cosmetic fixes taken:

| Severity | Finding | Resolution |
|---|---|---|
| Low | Green *solids* (`scoreSolid` ≥7, `RECOMMENDATION_META.Proceed.bar`) still used emerald — a second green next to proof-green | Swapped to `bg-proof` |
| Low (noted) | The MOCK/LIVE header pill still uses emerald | Out of scope — it's dev chrome, gated off by default (M0) |

## Remaining gaps / follow-ups

- The palette collapse is enforced at the token layer (`lib/ui.ts`), so all badge/dot/bar consumers inherit
  it. Any component that still hard-codes a rainbow Tailwind class directly (rather than going through a
  Tone) would be caught by the review's grep — see findings.
- Motion tokens: entrances already use transform-only `animate-fade-in`; a formal motion-token scale is a
  larger design-system task and is **not** attempted here (noted honestly rather than half-done).
- A full screen-by-screen visual pass in a browser is recommended alongside M3/M7's visual check.

## Testing steps (manual)

1. `npm run dev` → `/roleproof` board: status chips are now neutral/amber/green only; the stage is read from the rail, not colour.
2. A lead workspace: the next-action CTA uses the single proof accent; the verdict still shows Proceed/Borderline/Hold/Not-recommended in green/amber/neutral/rose.
3. The coach stage shows one trust statement, not three.

## Required checks before deploy

- No migration.
- `npm run typecheck` · `npm run build` · `npm test` green.
